# meta-signal-repository-ledger

Meta Signal contract for privileged repository-ledger configuration.

The ordinary event/query surface lives in `signal-repository-ledger`; this crate
is for meta-signal mutation of repository policy.
